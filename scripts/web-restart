#!/bin/sh
set -eu
if [ "$(id -u)" -ne 0 ]; then
    echo "Please run as root"
    exit 1
fi

systemctl --user -M web@ daemon-reload
systemctl --user -M web@ restart overleaf-pod.service caddy.service
